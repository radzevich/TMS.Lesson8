using System;

namespace Samples
{
    class Program
    {
        static void Main(string[] args)
        {
            // // Use enumerator of inner array
            // var daysOfWeekV1 = new DaysOfWeek();
            // foreach (var dayOfWeek in daysOfWeekV1)
            // {
            //     Console.WriteLine(dayOfWeek);
            // }
            //
            // // Use custom enumerator
            // var daysOfWeekV2 = new DaysOfWeekWithCustomEnumerator();
            // foreach (var dayOfWeek in daysOfWeekV2)
            // {
            //     Console.WriteLine(dayOfWeek);
            // }
            //
            // // Infinite list of number
            // var infiniteListOfNumber = new InfiniteNumbers();
            // foreach (var number in infiniteListOfNumber)
            // {
            //     Console.WriteLine(number);
            // }
            //
            // // Infinite generic list of int numbers
            // var infiniteGenericListOfNumbers = new InfiniteNumbersGeneric();
            // foreach (var number in infiniteGenericListOfNumbers)
            // {
            //     Console.WriteLine(number);
            // }
        }
    }
}